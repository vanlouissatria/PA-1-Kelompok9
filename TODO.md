# TODO - Perbaiki tampilan gambar galeri dari admin

- [ ya] Analisis penyebab gambar tidak tampil (path asset vs file upload, kolom `gambar` berisi apa)
- [ tidak] Perbaiki Blade `resources/views/admin/galeri/index.blade.php` agar `src` sesuai dengan nilai `gambar` (tanpa/ dengan `uploads` yang dobel)
- [tidak ] (Opsional) Tambahkan fallback jika file tidak ada (pakai `file_exists`)
- [ tidak] Jalankan test ringan: pastikan halaman admin galeri menampilkan thumbnail yang benar

perbaiki
